type Config = {
  mediaPositionerWidth: number;
  defaultOverlayDuration: number;
  defaultPauseDuration: number;
  defaultSkipDuration: number;
};

const config: Config = {
  mediaPositionerWidth: 320,
  defaultOverlayDuration: 5,
  defaultPauseDuration: 5,
  defaultSkipDuration: 3000,
};

export default config;
